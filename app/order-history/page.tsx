export default function OrderHistory() {
  // As requested: throw an error when this page is visited
  throw new Error("To nie jest jeszcze obsługiwane.");

  // (Unreachable) keep the original markup for reference
  return (
    <div>
      <h2>Historia zakupów</h2>
      <p>To jest strona historii zamówień (order-history).</p>
    </div>
  );
}
